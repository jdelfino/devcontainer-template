# Beads Configuration
# All settings can also be set via environment variables (BD_* prefix)
# or overridden with command-line flags. Run `bd <command> --help` for details.

# Git branch for beads sync commits
sync-branch: "beads-sync"
